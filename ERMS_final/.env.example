# Copy to apps/api/.env and packages/db/.env, fill in real values. Never commit .env.

# packages/db + apps/api
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/expense_reimbursement?schema=public"

# apps/api
PORT=4000
JWT_ACCESS_SECRET="change-me"
JWT_REFRESH_SECRET="change-me-too"
# Hard ceiling on the JWT itself - re-login required after this regardless
# of activity. The real session boundary for an active user is
# SESSION_IDLE_TIMEOUT_MIN below, enforced via Employee.lastActiveAt, not
# this value - keep ACCESS_TOKEN_TTL_MIN comfortably above it.
ACCESS_TOKEN_TTL_MIN=480
SESSION_IDLE_TIMEOUT_MIN=20
MFA_ISSUER="ExpenseRMS"

# apps/web
VITE_API_BASE_URL="http://localhost:4000/api"
